/** Mirrors the `numeric(20,4)` column type used for every money value (see supabase/migrations). */
export const MONEY_DECIMAL_PLACES = 4;
export const MONEY_MAX_INTEGER_DIGITS = 16;
export const MONEY_MAX_TOTAL_DIGITS = 20;
